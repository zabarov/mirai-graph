# Identity And Access Package

The platform needs account registration, role assignment and scoped API tokens.
Editors can create drafts, reviewers can approve publication and administrators
can manage roles.

Open issue: API tokens must be auditable because notification delivery may act
on behalf of a user.
