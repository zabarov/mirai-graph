# Mirai 2.1 Federation Graph-Native Pilot

Status: synthetic engineering evidence; independent and scientific review pending

This public-safe pilot compares the existing Mirai 2.0 explicit Federation
program with a Mirai 2.1 graph-resolved activation. The graph selects routing,
two owner paths, a technology-quality gate and a Kaizen closure. The two owner
paths form a deterministic parallel frontier.

The pilot performs no network, production or canonical graph writes. It uses
pure programs and the governed runtime only. Its result proves contract and
execution behavior for this fixture; it does not prove organization-wide
effectiveness or model-quality improvement.

```bash
npm run validate:mirai-2.1-federation-pilot
```

Regenerate committed public-safe evidence only through the controlled command:

```bash
node packages/cli/generate-mirai-2.1-federation-pilot.js --write
```
