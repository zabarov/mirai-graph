# Mirai Graph 1.0.0-rc.5

Status: release-candidate
Date: 2026-06-27

## Summary

`1.0.0-rc.5` adds Semantic Intent Resolution as an input-control extension
contract and adds Goal Vector Quality Control as a quality-control extension
contract.

The contract turns a free-form request into a governed decision before
execution starts:

```text
request
-> intent hypothesis
-> work scale
-> owner/process decision
-> context-slot check
-> clarification, brief, workflow restore or safe execution
```

This is not a new standalone profile and not a knowledge-storage layer. It is a
control boundary that helps projects, organizations and AI employees avoid
starting the wrong process from an ambiguous request.

## Added

- Standard document:
  - [Semantic Intent Resolution](../standard/semantic-intent-resolution.md)
- Schema:
  - [semantic-intent-resolution.schema.json](../schemas/semantic-intent-resolution.schema.json)
- Synthetic example:
  - [semantic-intent-resolution-minimal](../examples/semantic-intent-resolution-minimal/README.md)
- Negative fixtures for:
  - low confidence allowing execution;
  - generated proposal treated as canonical update.
- Goal Vector Quality Control standard:
  - [Goal Vector Quality Control](../standard/goal-vector-quality-control.md)
- Schema:
  - [goal-vector-quality-control.schema.json](../schemas/goal-vector-quality-control.schema.json)
- Synthetic example:
  - [goal-vector-quality-control-minimal](../examples/goal-vector-quality-control-minimal/README.md)
- Negative fixtures for:
  - missing batch vector link;
  - completion without evidence;
  - tests treated as acceptance;
  - missing reverse audit;
  - correction finding without route.

## Changed

- `package.json` version is now `1.0.0-rc.5`.
- CLI validation supports:

```bash
node packages/cli/validate-mirai-graph.js semantic-intent-resolution examples/semantic-intent-resolution-minimal/results/semantic-intent-resolution.json
```

- `npm test` includes Semantic Intent Resolution positive and negative checks.
- `npm test` includes Goal Vector Quality Control positive and negative
  checks.
- README and documentation map now list the contract in the core standard
  navigation.

## Validation

Release consolidation checks:

```bash
npm run validate:semantic-intent-resolution
npm run test:semantic-intent-resolution-negative
npm run validate:goal-vector-quality-control
npm run test:goal-vector-quality-control-negative
npm test
npm run release:check
git diff --check
```

## Claim Boundary

This release validates the public shape and safety boundaries of Semantic
Intent Resolution and Goal Vector Quality Control. It does not prove that AI
agents always choose the correct process, does not prove broad real-world drift
reduction, does not authorize live or destructive actions, does not update
canonical graph state from generated briefs, evidence, audits or proposals and
does not prove production readiness.
