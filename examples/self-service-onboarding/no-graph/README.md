# Synthetic Node Project Without Mirai Graph

This fixture represents a small software project before Mirai Graph adoption.

It intentionally has no `mirai-graph-package.json`, `graph/` or `gates/`
artifacts.
