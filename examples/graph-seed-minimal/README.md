# Minimal Graph Seed Example

This example shows a minimal Mirai Graph seed.

Validate it from the repository root:

```bash
node packages/cli/validate-mirai-graph.js seed examples/graph-seed-minimal/graph-seed.json
```
