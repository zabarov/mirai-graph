# Semantic Intent Resolution Minimal Example

This synthetic example shows how Mirai Graph can resolve a free-form request
before execution starts.

The example does not authorize work, update canonical graph state or bypass
gates. It only records a governed intake decision.

Validate:

```bash
npm run validate:semantic-intent-resolution
```
